
const stateInicial = {
    allFoods: [],
    allFoodsCopy: [],
}

const reduce = (state = stateInicial, action) => {
    switch (action.type) {
        default:
          return {...state}
    }
}

export default reduce;